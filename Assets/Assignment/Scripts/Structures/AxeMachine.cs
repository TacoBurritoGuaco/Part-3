using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//inherits directly from structure
//Doesn't need much else because structure already is made with axe machine in mind specifically
//However, this class exists to show that the structure code works with inheritence for V1
public class AxeMachine : Structure
{

}
